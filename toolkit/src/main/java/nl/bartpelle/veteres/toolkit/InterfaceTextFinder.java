package nl.bartpelle.veteres.toolkit;

import com.google.common.primitives.Bytes;

import java.nio.charset.Charset;
import java.util.Scanner;

/**
 * Created by Bart on 9/18/2015.
 */
public class InterfaceTextFinder {

	public static void main(String[] args) {
		/*DataStore ds = new DataStore("data/filestore", true);
		byte[] find = new Scanner(System.in).nextLine().trim().getBytes(Charset.forName("UTF-8"));

		for (int i = 0; i <= ds.getIndex(3).getLastArchiveId(); i++) {
			byte[] b = Compression.decompressArchive(ds.getIndex(3).getArchive(i));
			if (b != null && Bytes.indexOf(b, find) != -1) {
				System.out.println("Found occurrence in interface " + i + ".");
			}
		}*/
	}
}